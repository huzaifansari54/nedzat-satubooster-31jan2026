// src/sockets/notification.socket.js — Notification Socket Handlers
// -------------------------------------------------

/**
 * Handle notification socket events
 * @param {import('socket.io').Server} io
 * @param {import('socket.io').Socket} socket
 */
module.exports = function notificationSocket(io, socket) {
    // const uid = socket.user.id;

    // TODO: Add notification-specific events
    // socket.on('notifications:read', (ids) => { ... });
};
